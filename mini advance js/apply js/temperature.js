function temperatureConvert(temperature) {
    let fahrenheit=temperature * 9 / 5 + 32;
    let convert=fahrenheit;
    return convert;
}
var peramiter=40;
var add=temperatureConvert(peramiter); 
console.log(add);

