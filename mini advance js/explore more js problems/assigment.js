// 1. convert redian to degree
// 2.chack js file 
// isJavaScriptFile

// 3.oial price
/* 
disel --->140 tk
petrol--->150 tk
octail--->170 tk
*/

// best friend
// {name:"ratul", friend:"mamun",}
// {name:"mamun", friend:"ratul",}

// 1

/* function radianToDegree(radian) {
    let degree=57.296;
    let count=radian*degree;
    return count;
}

let radianPerameter = 2;

let add = radianToDegree(radianPerameter);
console.log(add); */

// 2.File.js

// let file=["file.pdf","file.html","file.css","file.js"];

// function isJavaScriptFile(jsFile) {
//     for (let i = 0; i < jsFile.length; i++) {
//         const element = jsFile[i];
//        if (element.includes("file.js")) {
//         return true;
//        } 
//     }
//     return false;
// }
// let filePeremeter=file;

// let add = isJavaScriptFile(file);
// console.log(add);

/* function oilPrice(disel,petrol,octail) {
    let diselPrice=140;
    let petrolPrice=150;
    let octailPrice=170;
    let Total=disel*diselPrice+petrol*petrolPrice+octail*octailPrice;
    return Total;

}

let diselPeremeter=3;
let petrolPeremeter=2;
let octailPeremeter=2;

let add=oilPrice(diselPeremeter,petrolPeremeter,octailPeremeter);

console.log(add); */

/* let friends = [
    { name: "mamun", friend: "ratul" },
    { name: "ratul", friend: "mamun" }
];

function BestFriend(bestFd) {
    for (let i = 0; i < bestFd.length; i++) {
        const element = bestFd[i];
        if (element.name.friend==element.friend.name) {
            return true;
        }
    }
    return false;
}

let result = BestFriend(friends);
console.log(result); */
